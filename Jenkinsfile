pipeline {
    agent any

    stages {

        stage('Show Information') {
            steps {
                sh 'echo Hello Thanush'
                sh 'date'
                sh 'hostname'
                sh 'whoami'
            }
        }

    }
}
